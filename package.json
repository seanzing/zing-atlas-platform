{
  "name": "atlas",
  "version": "1.0.0",
  "description": "ZING Atlas Platform",
  "scripts": {
    "build": "cd platform && npm ci && npx prisma generate && npm run build",
    "start": "cd platform && npm run start"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
